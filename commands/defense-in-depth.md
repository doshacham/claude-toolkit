---
description: Multi-layer validation — entry, business, environment, debug layers after fixing data bugs
argument-hint: [bug that was fixed, needs layered validation]
---

Invoke the defense-in-depth skill to add validation at every layer.

Skill(skill="defense-in-depth")

BUG FIXED: $ARGUMENTS
